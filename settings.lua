return {

	-- toggles on-screen debug information
	debug = false,
	profile = false,

	-- onscreen menu for mobile debugging
	menu = false,

	-- if enabled application stalls on error messages
	errors = true,
	
	-- anti aliasing (if false, floors all draw calls to ints)
	floor = true,

	-- viewport overlay
	overlay = false,
	culling = true,

	-- add outline markers for chunks and groups during scene generation
	chunks = false,
	labels = false,
	outlines = false,

	-- fun stuff
	slowmo = false,

}
